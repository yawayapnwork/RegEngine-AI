[build-system]
requires = ["setuptools>=68", "pybind11>=2.12"]
build-backend = "setuptools.build_meta"

[project]
name = "regengine-native"
version = "1.0.0"
description = "RegEngine AI ultra-low-latency native policy evaluation kernel (C++/pybind11)."
requires-python = ">=3.9"
