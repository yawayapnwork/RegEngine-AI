# Dataset formatting utilities only need the stdlib + what's already in
# the main requirements.txt (pydantic). Everything below is specific to
# the training/export/serving stages of this pipeline.

torch>=2.3.0
transformers>=4.44.0
peft>=0.12.0
bitsandbytes>=0.43.1
trl>=0.9.6
datasets>=2.20.0
accelerate>=0.33.0
flash-attn>=2.6.0 ; platform_system == "Linux"  # no prebuilt wheels for Windows/macOS; training is Linux/CUDA-only anyway
sentencepiece>=0.2.0
httpx>=0.27.0

# Not imported directly by any script here -- installed separately as the
# vLLM serving container (see vllm/docker-compose.vllm.yml, image
# vllm/vllm-openai). Listed for completeness if running vLLM bare-metal
# instead of via the compose file.
# vllm>=0.6.3
