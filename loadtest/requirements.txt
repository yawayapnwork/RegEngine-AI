# Load-testing toolchain only -- deliberately separate from the main
# requirements.txt since none of this ships in the production API image.
locust>=2.29.0
PyYAML>=6.0
httpx>=0.27.0
redis>=5.0
bcrypt>=4.1
